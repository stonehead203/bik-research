services:
  - type: web
    name: bik-research
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn company_analysis_app:app --bind 0.0.0.0:$PORT
